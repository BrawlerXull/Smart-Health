import 'package:get/get.dart';

import '../controllers/fitbit_controller.dart';

class FitbitBinding extends Bindings {
  @override
  void dependencies() {
    Get.find<FitbitController>(

    );
  }
}
